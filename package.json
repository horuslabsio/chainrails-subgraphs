{
    "name": "chainrails-subgraph",
    "version": "1.0.0",
    "description": "Chainrails subgraph for monitoring intent funding",
    "main": "index.js",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "prepare": "node scripts/generate-subgraph.js",
        "prepare:arbitrum-one": "yarn prepare mainnet arbitrum-one",
        "prepare:arbitrum-sepolia": "yarn prepare testnet arbitrum-sepolia",
        "prepare:avalanche": "yarn prepare mainnet avalanche",
        "prepare:avalanche-testnet": "yarn prepare testnet avalanche-testnet",
        "prepare:base": "yarn prepare mainnet base",
        "prepare:base-sepolia": "yarn prepare testnet base-sepolia",
        "prepare:bsc": "yarn prepare mainnet bsc",
        "prepare:mainnet": "yarn prepare mainnet mainnet",
        "prepare:sepolia": "yarn prepare testnet sepolia",
        "build:arbitrum-one": "yarn prepare:arbitrum-one && yarn codegen && yarn build",
        "build:arbitrum-sepolia": "yarn prepare:arbitrum-sepolia && yarn codegen && yarn build",
        "build:avalanche": "yarn prepare:avalanche && yarn codegen && yarn build",
        "build:avalanche-testnet": "yarn prepare:avalanche-testnet && yarn codegen && yarn build",
        "build:base": "yarn prepare:base && yarn codegen && yarn build",
        "build:base-sepolia": "yarn prepare:base-sepolia && yarn codegen && yarn build",
        "build:bsc": "yarn prepare:bsc && yarn codegen && yarn build",
        "build:mainnet": "yarn prepare:mainnet && yarn codegen && yarn build",
        "build:sepolia": "yarn prepare:sepolia && yarn codegen && yarn build",
        "deploy": "node scripts/deploy-subgraph.js",
        "deploy:arbitrum-one": "node scripts/deploy-subgraph.js arbitrum-one",
        "deploy:arbitrum-sepolia": "node scripts/deploy-subgraph.js arbitrum-sepolia",
        "deploy:avalanche": "node scripts/deploy-subgraph.js avalanche",
        "deploy:avalanche-testnet": "node scripts/deploy-subgraph.js avalanche-testnet",
        "deploy:base": "node scripts/deploy-subgraph.js base",
        "deploy:base-sepolia": "node scripts/deploy-subgraph.js base-sepolia",
        "deploy:bsc": "node scripts/deploy-subgraph.js bsc",
        "deploy:mainnet": "node scripts/deploy-subgraph.js mainnet",
        "deploy:sepolia": "node scripts/deploy-subgraph.js sepolia"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.67.4",
        "@graphprotocol/graph-ts": "0.32.0",
        "mustache": "^4.2.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
